.container
  .row
    .col.s12.m6.offset-m3
      .card.blue-grey.darken-1
        = @blogs.each do |blog|
          .card-content.white-text
            %span.card-title 
              = blog.sentence
            .user-name-div
              %span 
                = blog.user.name
            %p
          .card-action
            %a(href="#")SHOW
            %a(href="#")EDIT
            %a(href="#")DESTROY
        -# = @blogs.each do |blog|
      -# = render "blogs/sentencecards"
        
          
          